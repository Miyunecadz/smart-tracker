POSTGRES_USER=smart_tracker
POSTGRES_PASSWORD=password
POSTGRES_DB=smart_tracker

# GitHub MCP — fine-grained PAT, this repo only, Issues read (+write if needed).
# Create: https://github.com/settings/personal-access-tokens/new
GITHUB_PERSONAL_ACCESS_TOKEN=
